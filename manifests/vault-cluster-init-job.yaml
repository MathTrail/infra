# ServiceAccount for the vault-cluster-init Job.
# Needs no special permissions itself — Vault does the TokenReview via its own SA.
apiVersion: v1
kind: ServiceAccount
metadata:
  name: vault-cluster-init

---
# Job: configure cluster-wide Vault settings after it starts in dev mode.
# Enables Kubernetes auth, Database Secrets Engine, and ESO policy/role.
# Runs once; RestartPolicy=OnFailure handles transient startup races.
apiVersion: batch/v1
kind: Job
metadata:
  name: vault-cluster-init
spec:
  ttlSecondsAfterFinished: 300
  template:
    spec:
      restartPolicy: OnFailure
      serviceAccountName: vault-cluster-init
      containers:
        - name: vault-cluster-init
          image: hashicorp/vault:1.17
          env:
            - name: VAULT_ADDR
              value: "http://vault.vault.svc.cluster.local:8200"
            - name: VAULT_TOKEN
              value: "root"
          command: ["/bin/sh", "-c"]
          args:
            - |
              set -e

              echo "Waiting for Vault to be ready..."
              until vault status 2>/dev/null; do
                echo "  vault not ready, retrying in 3s..."
                sleep 3
              done
              echo "Vault is ready."

              # ── Kubernetes auth ──────────────────────────────────────────
              vault auth enable kubernetes 2>/dev/null || echo "kubernetes auth already enabled"

              vault write auth/kubernetes/config \
                kubernetes_host="https://${KUBERNETES_SERVICE_HOST}:${KUBERNETES_SERVICE_PORT}"

              # ── Database Secrets Engine ──────────────────────────────────
              vault secrets enable database 2>/dev/null || echo "database secrets already enabled"

              # ── Policy for ESO ───────────────────────────────────────────
              vault policy write eso-policy - <<'POLICY'
              path "database/creds/mentor-api-role-*" {
                capabilities = ["read"]
              }
              POLICY

              # ── Kubernetes auth role for ESO's ServiceAccount ────────────
              vault write auth/kubernetes/role/eso-role \
                bound_service_account_names="external-secrets" \
                bound_service_account_namespaces="external-secrets" \
                policies="eso-policy" \
                ttl="24h"

              echo "Vault cluster init complete."

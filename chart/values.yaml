# Group-level deploy toggles.
# Each group controls a set of ArgoCD Applications that are deployed together.
#
# To enable chaos testing (staging only):
#   helm upgrade infra-apps ./chart --set chaos.deploy=true

core:
  deploy: true

chaos:
  deploy: false

storageclass:
  deploy: true

apiVersion: redhatcop.redhat.io/v1alpha1
kind: DatabaseSecretEngineConfig
metadata:
  name: mentor-postgres
  namespace: vault-config-operator
  annotations:
    argocd.argoproj.io/sync-wave: "1"
spec:
  vaultAuthRef: vault-admin-auth
  path: database
  pluginName: postgresql-database-plugin
  connectionName: mentor-postgres
  allowedRoles:
    - mentor-api-role
  connectionURL: "postgresql://{{username}}:{{password}}@postgres-postgresql.mathtrail.svc.cluster.local:5432/mentor?sslmode=disable"
  rootCredentials:
    username: postgres
    passwordSecret:
      name: postgres-postgresql
      namespace: mathtrail
      key: postgres-password
